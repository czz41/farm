spring:
  application:
    name: springboot-init
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sky_take_out
    username: takeout
    password: 123456
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  # session 失效时间（分钟）
  session:
    timeout: 86400
    store-type: none
  # redis 配置
  redis:
    port: 6379
    host: localhost
    database: 0
  # 邮件配置（QQ SMTP）
  mail:
    host: smtp.qq.com
    port: 465
    username: 1749127251@qq.com
    password: ezpvmxiuffiabhba
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
server:
  port: 7529
  servlet:
    context-path: /api
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)

# DeepSeek AI 配置
deepseek:
  api-key: sk-74d38987675b473c930fb97739dd1f08
  base-url: https://api.deepseek.com/v1
  model: deepseek-chat
  timeout: 60000 # 请求超时（毫秒）

# MQTT 下发配置
mqtt:
  topic: irr/device/001/cmd

# 和风天气配置
qweather:
  api-key: 32100eec29e74b4e97b1c897b086fd31
  base-url: https://devapi.qweather.com